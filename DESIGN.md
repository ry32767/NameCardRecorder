# DESIGN — NameCardRecorder

> このファイルはデザイン判断の唯一の正であり、将来のセッションへの引き継ぎ書。
> UI を触るセッションは、まずこれを読んでから作業する。更新したら変更履歴に 1 行残す。
> 値の固定先は [tailwind.config.js](tailwind.config.js) と [src/index.css](src/index.css)。ここは**根拠**の記録。

## 題材（1文）

もらった紙の名刺を撮って、自分ひとりの private な台帳に積んでいくアプリ。使うのは本人だけで、
各画面の唯一の仕事は — 一覧：**探して見つける** / 登録：**撮って直して保存する** / 詳細：**その人に連絡する** / 設定：**データ置き場に繋ぐ**。

## トークン

| 種別 | 名前 | 値 | 根拠（なぜこれか） |
|---|---|---|---|
| color | `ink` | `#131A22` | 純黒にしない。名刺の印刷インクの黒は青みを含む。paper 比 15.7:1 |
| color | `ink-soft` | `#55626E` | 副次テキスト。paper 比 5.60:1（AA 本文） |
| color | `ink-faint` | `#64707A` | メタ情報。**薄いグレーが基準割れしがちなので 4.5:1 を割らない値まで濃くした**（paper 比 4.54:1） |
| color | `paper` | `#F1F3EF` | 背景。名刺用紙のわずかに緑みを帯びた白。無彩色にしない |
| color | `card` | `#FFFFFF` | カード面。紙が paper の上に載っている状態 |
| color | `rule` | `#D7DCD3` | 装飾的なヘアラインのみ（1.25:1、非情報） |
| color | `rule-strong` | `#868F82` | 入力欄など**見えている必要がある境界**。card 比 3.35:1（WCAG 1.4.11） |
| color | `indigo` | `#2C4C7C` | 主アクセント。**藍は名刺の刷り色の定番**。card 比 8.64:1、白抜き文字も 8.64:1 |
| color | `indigo-deep` / `indigo-tint` | `#1D3357` / `#E7ECF4` | hover / 選択中の面 |
| color | `vermilion` | `#B23B27` | 破壊的操作とエラー。**朱肉**の色。card 比 5.92:1 |
| color | `moss` | `#3F6B4A` | 成功。card 比 6.15:1 |
| color | `edge-*` | 藍/苔/砂/紫紺/鉄/朱 の 6 色 | 小口帯（下記シグネチャ）。**自由な HSL を生成せず、この 6 色から決定論的に選ぶ**。全色 card 比 4.99:1 以上 |
| type (display) | `font-sans` 700 / tracking -0.02em | 日本語ゴシックの system stack | 下記「書体を DL しない判断」 |
| type (body) | `font-sans` 400 / 16px / line-height 1.6 | 同上 | 入力欄も 16px を下回らない（iOS 自動ズーム回避） |
| type (signature) | `font-mincho` | 明朝の system stack | **氏名の表示だけ**に使う。実物の名刺が明朝で刷られていることに由来する |
| type (utility) | `font-mono` | 等幅 system stack | 電話・郵便番号・日付・Issue 番号。桁が揃うと台帳として読みやすい（実用と題材の両方に効く） |
| space | 4px 基調（Tailwind 既定） | 4/8/12/16/24/32 | 画面の主役が「紙のカード」なので余白は詰めすぎない |
| radius | `control` 4px / `card` 6px | | 名刺の角はほぼ直角。角丸を効かせすぎると紙に見えなくなる |
| shadow | `card` / `lift` / `sheet` | 上記 config 参照 | 紙が 1 枚持ち上がった程度に留める |
| motion | 150ms ease-out | | `prefers-reduced-motion` を尊重（`src/index.css` で一括無効化） |

### 書体を DL しない判断（重要）

日本語 webfont は 1 ファイルで数 MB ある。このアプリは Tesseract の言語データ（10–15MB）を初回に落とすため、
そこに書体を積むとスマホの初回起動が壊れる（`docs/spec.md` の性能要件に直撃する）。
よって **display 書体を追加ダウンロードしない**と決め、区別は weight・tracking・明朝/等幅のロール分けだけで作る。
「Inter 一択」に落ちないための代替として、**明朝ロールと等幅ロールを題材に紐づけて使い分ける**のがこのプロジェクトの型。

## シグネチャ

**名刺の実寸比（91:55）と、左端の小口帯。**

- 一覧のカードは日本の標準名刺と同じ **91:55**（`aspect-meishi`）。等間隔の汎用カードグリッドではなく、
  名刺入れを開いて中身を上から見ている状態のメタファにする。
- 各カードの左端に 4px の色帯（`edge-*`）を敷く。色は**会社名から決定論的に**決まる（同じ会社は常に同じ色）。
  名刺入れの中で、同じ会社の紙が同じ色に見える状態を作るのが狙い。
- 詳細画面の氏名だけが明朝。実物の名刺を開いた感じに寄せる。

大胆さはここに集中させ、周りは静かに保つ。装飾的なアイコン・グラデーション・イラストは足さない。

## 避けると決めた初期値

- **クリーム地 + テラコッタ（`#D97757` 系）を使わない** — AI が作った兆候として最も読まれる型。名刺の紙は暖色クリームではなく、わずかに冷たい白。
- **Tailwind 既定の `gray-*` / `blue-*` を使わない** — `tailwind.config.js` で `theme.colors` を**拡張ではなく置き換え**にして、既定色を機械的に使えなくしてある。
- **Inter を含む Latin display 書体を読み込まない** — 上記「書体を DL しない判断」。
- **紫グラデーション・glassmorphism を使わない** — 題材（紙・印刷・台帳）が要求していない。
- **見出しや機能ラベルに絵文字を置かない** — 個人情報を扱う台帳の画面にふさわしくない。
- **`01 / 02 / 03` の装飾連番を使わない** — 登録フローのステップ表示のように順序が実際の情報を持つ場合のみ数字を出す。

## 不変条件（UI を触るとき絶対に壊さない制約）

1. **色・書体・余白・角丸・影をベタ書きしない。** 必ず `tailwind.config.js` のトークンから引く。任意の hex を JSX/CSS に書かない。
2. **一覧カードのアスペクト比 91:55 を変えない。** 中身が入らないなら文字を減らす／省略するほうを選ぶ。
3. **タップ領域は 44×44px 以上**（`min-h-tap` / `min-w-tap`）。アイコンだけのボタンも例外なし。
4. **入力欄のフォントサイズを 16px 未満にしない**（iOS の自動ズームで体験が壊れる）。
5. **幅 375px で横スクロールを発生させない。** 固定 px 幅を置かない、画像は `max-width:100%`、長い URL・メールは折り返す。
6. **`dangerouslySetInnerHTML` を使わない。** PAT を localStorage に置く以上、XSS を持ち込まないことが唯一の防壁（`docs/architecture.md`）。ESLint で機械的に禁止済み。
7. **トークン・氏名・連絡先を DOM 属性や `console` に出さない。** 保存済みトークンを input の `value` に戻さない（マスク表示専用の要素で見せる）。
8. **状態を色だけで表さない。** エラー・成功にはテキスト（と必要ならアイコン）を必ず併記する。
9. **キーボードフォーカスを消さない。** `outline: none` を単独で書かない（`src/index.css` の `:focus-visible` リングを使う）。
10. **hover にしか無い操作を作らない。** タッチにホバーは無い。

## 対象画面と既存機能インベントリ

> リデザイン時はここを 1 項目ずつ実際に操作して機能後退がないことを確認する。スクリーンショットだけの確認は不可。
> 各項目の合格ラインは [docs/spec.md](docs/spec.md) の受け入れ条件が正。

- **一覧 `#/`**: カード一覧の表示／検索／ラベル絞り込み（会社・イベント・タグ）／絞り込みクリア／「+ 名刺を追加」／設定へ／カードを開いて詳細へ／空状態・0 件・オフライン表示
- **登録 `#/new`**: カメラ撮影／画像ファイル選択（**表・裏の 2 枚まで**・面ごとに外せる）／面ごとの OCR 進捗表示／**読み取った文字の行タップコピー**（コピー失敗時は手動選択欄）／フォームの全欄編集／バリデーション（メール形式・氏名か会社は必須）／保存（ラベル → 画像 → Issue の順）／二重送信の抑止／キャンセルして戻る
- **詳細 `#/cards/:number`**: 全フィールド表示／名刺画像（表・裏）の拡大／`mailto:` / `tel:` リンク／GitHub で開く／一覧へ戻る／存在しない番号の 404 表示
- **設定 `#/settings`**: リポジトリ入力／トークン入力（マスク表示）／保存／接続テスト（成功・401・404 の出し分け）／設定を消去

## 変更履歴

- 2026-09-08: OCR の自動振り分けを廃止し「読み取った行をタップしてコピー」に変更。名刺画像を表・裏の 2 枚対応に。登録画面のインベントリを更新。
- 2026-09-08: 初版。題材を「紙の名刺を積む台帳」に固定し、藍＋紙白のパレット、system 書体（明朝／等幅のロール分け）、シグネチャを「名刺比 91:55 のカード＋会社色の小口帯」と決定。全色ペアのコントラストを計算して AA を確認済み。
